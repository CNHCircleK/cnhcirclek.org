{% extends "page.twig" %}

{% block post_header_left %}
    <h1>{{ post.title }}</h1>
    <p>{{ post.post_date|date }}</p>
{% endblock %}

{% block post_header_right %}
    <img src="{{ post.author.avatar }}" alt="{{ post.author.name }}" class="img-circle author__avatar">
    <div class="author">
        <div class="author__name">{{ post.author.name }}</div>
        <div class="author__other">{{ post.author.position }}</div>
    </div>
{% endblock %}

{% block after_content %}
    {% if post.comment_status != 'closed' %}
        <div id="comments">
            <div class="row">
                <div class="col-xs-12">
                    {{ fn('comments_template') }}
                </div>
            </div>
        </div>
    {% endif %}
{% endblock %}
