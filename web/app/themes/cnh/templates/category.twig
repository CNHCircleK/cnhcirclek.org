{% extends "base.twig" %}

{% block content %}
    <div class="container container_white">
        <h1>{{ function('single_cat_title') }}</h1>
        {% for post in posts %}
            {% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
        {% endfor %}
    </div>
{% endblock %}